## [2025-12-22] - Рефакторинг и Миграция на PostgreSQL

**User:** "Мне нужно чтобы ты проанализировал проект, мне необходим рефакторинг данного кода. В первую очеередь нужно переработать всю базу данных, сервис использовал sqllite3 - мы будем мигрировать на PostgreSQL."

**System:**
- Миграция базы данных с SQLite на PostgreSQL.
- Создание схемы БД `migration/init.sql` с таблицами `categories`, `stores`, `coupons`.
- Рефакторинг слоя `storage`: переход на драйвер `pgx` и использование `model.Coupon`.
- Рефакторинг `collector`: реализация парсинга `lovikod.ru` с поддержкой категорий и магазинов.
- Обновление `snbot` для работы с новыми моделями данных.
- Внедрение конфигурации через `config.yaml` с разделением параметров подключения к БД.
- Docker: обновление `Dockerfile` до Go 1.21, добавление `docker-compose.yml`.

- Добавлены библиотеки:
    - `github.com/jackc/pgx/v5` - PostgreSQL driver
    - `gopkg.in/yaml.v3` - YAML support
